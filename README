[compiler-design-eth-tests](https://github.com/liuzikai/compiler-design-eth-tests) is added as a git submodule.

After cloning this repo, run:
```shell
git submodule update --init
```

After each pull, run:
```shell
git submodule update
```